"""LLM-based job listing analysis.

This package handles matching job listings against a candidate profile using
a large language model. It contains the abstract adapter interface, concrete
LLM backends, and the orchestration logic that builds prompts and parses results.
"""
